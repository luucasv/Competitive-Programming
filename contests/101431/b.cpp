#include <bits/stdc++.h>
#define fr(a, b, c) for(int a = b, __ = c; a < __; a++)

using namespace std;
typedef long long ll;

int main(){
	ios::sync_with_stdio(0);
	int n;
	string s;
	cin >> n >> s;
	ll ans = 0;
	
}